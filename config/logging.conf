[loggers]
keys=root

[handlers]
keys=consoleHandler

[formatters]
keys=plain

[logger_root]
level=DEBUG
handlers=consoleHandler

[handler_consoleHandler]
class=StreamHandler
level=DEBUG
formatter=plain
args=[]

[formatter_plain]
format=%(asctime)s - %(levelname)s - %(message)s
